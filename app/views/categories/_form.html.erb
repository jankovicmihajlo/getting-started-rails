<%= form_with model: category do |form| %>
  <% if category.errors.any? %>
    <p style="color: red;"><%= category.errors.full_messages.first %></p>
  <% end %>

  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit %>
<% end %>